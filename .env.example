DATABASE_URL="postgresql:///nest_memory?host=/var/run/postgresql"
# Peer auth as your OS user (default after ./scripts/setup-database-postgres.sh).
#
# If you connect as the postgres role over TCP instead:
# DATABASE_URL="postgresql://postgres:postgres@localhost/nest_memory"
OPENAI_API_KEY="sk-..."
NEST_KNOWLEDGE=/data/nest-knowledge
