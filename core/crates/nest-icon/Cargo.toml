[package]
name = "nest-icon"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license = "MIT OR Apache-2.0"
repository.workspace = true
description = "Font Awesome icon fonts and widgets for Nest egui applications"

[dependencies]
egui = "0.31"
nest-core = { workspace = true }
nest-error = { workspace = true }

[features]
default = ["solid", "regular", "brands"]
solid = []
regular = []
brands = []

[dev-dependencies]
